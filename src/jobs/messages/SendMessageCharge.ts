import { Bill } from "../../@types/Bill";

export function SendMessageCharge(
  bill: Bill,
  debtorName: string,
  debtorContact: string,
  userName: string
) {
  // Construct message with this info
}
